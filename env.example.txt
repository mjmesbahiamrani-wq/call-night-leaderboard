# Copy this file to one named ".env" for a local preview (npm run local). On Vercel these are the
# project's Environment Variables — the deploy form asks for the first three.

# --- Required -------------------------------------------------------------
# Your Google Sheet, shared "Anyone with the link → Viewer". Paste the link
# from the address bar (or just the sheet ID). A "gid=" in the link picks a tab.
SHEET_URL=
# The password inside the board's link. Pick something long: letters and
# numbers, 20+ characters. The board opens at  https://<your-site>/#k=<TEAM_KEY>
TEAM_KEY=
# Your team blocks, left to right across the sheet, comma-separated.
TEAMS=Team A,Team B,Team C

# --- Optional ------------------------------------------------------------
# Weekly conversions goal: shows the progress bar.
GOAL_WEEK=
# Weekly prize for the #1 in conversions. The band only appears with an amount.
# PRIZE_LABEL: "giftcard" is translated (carte-cadeau / gift card); any other
# text shows as written.
PRIZE_AMOUNT=
PRIZE_LABEL=
CURRENCY=CAD
# Team time zone — "today" is computed here, never on the viewer's device.
TIMEZONE=America/Toronto
# Which evenings are call nights. The race week starts on the first of them.
CALL_NIGHTS=sat,sun,mon,tue
# When the night starts (24h) — shows a countdown on the title screen.
NIGHT_START=
# Small text above the title, and the line under it on the title screen.
KICKER=
SUBTITLE=

# --- Sheet layout (leave alone unless your sheet is shaped differently) ----
BLOCK_WIDTH=100
COLS_PER_PERSON=8
PEOPLE_PER_BLOCK=12
HEADER_DATE=Date
HEADER_CALLS=# of Calls
COL_CALLS=0
COL_MINUTES=1
COL_CONVERSIONS=3
COL_NOSHOWS=5
COL_TIME=7

# --- Faster refresh (optional, see README) ---------------------------------
SHEET_FAST_URL=
SHEET_FAST_MAX_STALE_MIN=15
